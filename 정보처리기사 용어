테스트오라클{
	샘플링오라클:몇몇 테스트케이스 입력값들만
	일관성검사오라클:변경이 있을때 전과후를 비교해 값이 동일한지
	참오라클:모든 입력값과 모든 오류검출
}

정보보안{
	기밀성:정보를 허가받지 않은 사람이 볼 수 없도록
	무결성:정보가 인가되지 않은 방식으로 변경되거나 삭제되지 않도록
	가용성:허가된 사용자가 필요할 때 언제든지 접근할 수 있도록
}

버스{
	주소버스:주소 지정
	데이터버스:데이터 전달
	제어버스:제어 신호 전달
}

운영체제시간{
	대기시간:프로세스가 준비 큐에서 CPU를 할당받기 위해 기다린 시간의 총합입니다.
	실행시간:프로세스가 CPU를 사용하여 실제로 작업을 수행하는 데 걸리는 시간입니다. (요리사가 요리를 실제로 만드는 시간 (예: 5분))
	반환시간:프로세스가 시스템에 도착한 시점부터 모든 작업을 마치고 종료될 때까지의 전체 시간입니다. (손님이 식당에 들어와서 음식을 주문하고, 기다리고, 먹고, 나갈 때까지의 시간
(예: 15분))
	응답시간:요청 후 첫 번째 응답이 시작될 때까지의 시간
	시간간격:CPU가 각 프로세스에 할당하는 고정된 시간 단위입니다.
}

정규화단계		주요 목적		해결하는 문제 예시
1NF			원자값으로 분해			한 셀에 여러 값이 있는 경우
2NF			부분 함수 종속 제거		복합키의 일부에만 종속된 속성
3NF			이행 함수 종속 제거		비기본키 속성이 다른 비기본키에 종속된 경우
4NF			다치 종속 제거		하나의 속성이 여러 독립적인 다중값 속성에 종속된 경우
5NF			조인 종속 제거		조인 종속으로 인해 데이터 중복이 발생하는 경우
보이스코드  모든 결정자가 후보키. 실제사례 없음.

CIDR(classless Inter-Domain Routing):기존 IPv4 클래스를 무시.네트워크주소와 호스트 주소를 임의로 구분
DHCP(Dynamic Host Configuration Protocol):몇개의 ip를 여러 사용자가 공유할수 있도록 인터넷에 접속할때마다 자동으로 ip 주소 동적할당.

Hash:데이터를 고정된 길이의 값이나 키로 변환. 무결성 검증.

그레이웨어:소프트웨어 제공자는 악의성은 없지만 사용자 입장에선 유용하지 않거나 악의적 스파이웨어,에드웨어,공유웨어.

서비스지향아키텍처:정보시스템을 공유와 재사용이 기능한 서비스 단위나 컴포넌트 중심으로 구축 아키텍처. 정보를 서비스 로 간주한 아키텍처.